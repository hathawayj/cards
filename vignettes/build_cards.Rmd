---
title: "Building Cards"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{Building Cards}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>"
)
```

```{r setup}

# https://web.byui.edu/FacultyClasses/ClassList


#### Latest Session Example   ######
### Could be fun to make a function that downloads the pdf documents without going to the web browser.  Not sure it is possible.
### Nice to compile this into a package.
# devtools::install_github("hathawayj/cards")
library(cards)
library(jpeg)
library(grid)
library(ggplot2)
dir_path <- pdf_2image(file = file)
jpg_files = fs::dir_ls(dir_path, regexp =  "jpg")
pdata = jpeg_roll(image_files = jpg_files)
gg_object = ggplot_faces(faces = pdata$all_faces,faces_names = pdata$all_names)
flashcard_pdf(gg_object,pdf_name = fs::path(dir_path, "Fall_2019.pdf"))
flashcard_pdf(gg_object,pdf_name = fs::path(dir_path, "Fall_2019_oneside.pdf"),front_back = FALSE)

```
